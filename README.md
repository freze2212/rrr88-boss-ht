# Landing Base

Scaffolded Vite + React + Tailwind CSS starter for simple landing pages.

Commands

- Install dependencies:

```powershell
npm install
```

- Start dev server:

```powershell
npm run dev
```

Build / preview

```powershell
npm run build; npm run preview
```
